# TODO

* heredoc broken, plus de prompt ??? Ctrl-D
* reprendre la structure des redirections pas de doublon, un seul heredoc, le dernier est enregistre dans la structure ? a verifier avec bash
* reprendre la gestion des errors.

##
	* 'cd -' the switching between folders dont work

### unset/export/env
	* export a=1; env; echo $a; unset a
		*sometimes work, sometines not (use valgrind) why ??

### valgrind
	* test "sad | das| | asd| Asd| sda |"
		* check: Invalid read of size 1
	* >><><>>
		* valgrind error and create 9 tokens instead of 5

### redirection structure
	* '> file echo hello' & 'echo hello > file' do the same
	* build a linked structure for all redirections in a process
	* add to tester the display for redirections

### ERRORS: 
	* check error and if any print and return shell
	* erreurs de syntaxe autour des redirections a reprendre

### $? 
	* valeur retour du dernier process

## signals handling
	* Block ^\ when Ctrl-\ is used

### PARSER:
	* echo"hello"
	* test't'
	* cd"-"
		will work with minishell but shouldn't, probably need to stick some token together when there is quotes.

### analyzer:
	* a|b|c|e|p|pw|ec|un|ex
		* builtin_command type evaluated, check the strcmp!

### EXECUTER:

### TESTER:

* tester using now the same set of test, should display only what is asked
* tester should work with valgrind the same way as the main program to test if everything is free


### Commenting, refactoring and cleaning code.
- []: ms_main.c
- []: ms_free.c
- []: ms_errors.c
	PARSER
	-[]: ms_paser.c
	-...
	analyzer
	-[]: ms_analyzer.c
	-...
	EXECUTER
	-[]: ms_executer.c
	-...
	BUILTINS
	-...


* range the structure for memory optimization, bigger to lower byte in order